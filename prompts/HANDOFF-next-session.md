# Handoff — продовження в новій сесії (крок 2: зібрати ТЗ)

> ✅ **СТАТУС (2026-06-12): КРОК 2 ЗАВЕРШЕНО.** Готові `specs/statok-tz.md` (повне ТЗ, 1599 рядків, 56 FR + повна арх-частина) і `tasks/backlog.md` (54 задачі `ST-001..054`, 12 епіків, повна трасування FR→ST). Адверсаріальне ревю пройдено; critical/major виправлено (коміт `ed696a6`). Чернетки `_draft-*.md` влиті в ТЗ і видалені (історія: `9fe05fc`, `8a98016`).
> **Наступне — крок 3 (імплементація)** за `prompts/03-manager-workflow.md` через `Workflow` (потребує явного opt-in користувача). Деплой — патерн tardis, прод не піднімати. Інструкція нижче — історичний запис задачі цієї сесії.
>
> Скопіюй блок нижче як перше повідомлення нової сесії Claude Code.
> Робоча директорія: `O:\projects\statok`.

---

Продовжуєш агентний пайплайн проєкту **Statok** (self-hosted single-user застосунок особистих фінансів; фаза 1 — інвестиції/портфель). Крок 1 (вимоги) ЗАВЕРШЕНО. Твоє завдання — крок 2: добити й зібрати повне ТЗ для агента, який далі АВТОНОМНО імплементуватиме систему. Відповідай українською.

`/notify-me` — активуй одразу (пінг при старті, на майлстоунах і при готовому ТЗ).

## Що ВЖЕ зроблено (не повторюй)

- **Вимоги зафіксовано**: 30 design-decisions питань відповіджено власником і дистильовано у **`specs/requirements.md`** — ДЖЕРЕЛО ІСТИНИ, не відхиляйся. Коміти: `d93f3b2` (requirements), `30c7e84` (повні Q&A в історії), `d08a870` (робочий файл видалено). Файлу `*.decisions.md` більше нема — не шукай і не відтворюй.
- **Архітектурна частина ТЗ готова**: **`specs/_draft-data-arch.md`** (612 рядків; писав підагент на 107k токенів, звірено з кодом tardis). Покриває: повну Drizzle/Postgres-схему, REST API, сервіси (valuation/PnL/bond/fx/snapshot), джоби (EOD-ланцюг 23:30 Києва: syncPrices Yahoo → syncFxRates Frankfurter+НБУ → автопогашення облігацій → dailySnapshot), бекапи (host cron pg_dump→age→rclone B2 + `GET /api/backup/dump`), монорепо (Bun усюди), фронтенд (composables без Pinia, uPlot, vue-i18n, PWA), env, безпеку (JWT jose 7d + sliding refresh).
  - Ключові арх-рішення: `asset_id NOT NULL` на всіх транзакціях (чисто грошові операції → авто-створюваний cash-актив валюти); фіат `bigint` minor units; transfer-пара з незалежними валютами ніг (покриває і валютний обмін); CHECK-матриця полів за типом транзакції; оверселл → 409; YTM — Newton–Raphson з bisection-fallback.
  - Свідомі спрощення v1 (зафіксовані в документі): НКД (accrued interest) не в оцінці; редагування історії не перебудовує минулі снапшоти (явний rebuild-ендпоінт); зміна пароля адміна поза UI.
- **Контекст власника** (з requirements.md): зараз тримає лише ОВДП → облігаційний модуль найвищий пріоритет; рахунки в UAH/USD/EUR; базова валюта USD; філософія — мінімальний MVP, архітектура не заважає розширенню.

## Що лишилось зробити (по порядку)

### 1. Функціональна частина ТЗ — запусти підагента (general-purpose) з промптом НИЖЧЕ ДОСЛІВНО

```
Ти — фінансовий продакт-аналітик. Пишеш функціональну частину ТЗ для Statok — self-hosted single-user застосунку обліку інвестицій/портфеля (Фаза 1). Документ призначений для агента, який АВТОНОМНО імплементуватиме систему — жодних «уточнимо пізніше», все має бути однозначно.

Прочитай спочатку:
- O:\projects\statok\specs\requirements.md — узгоджені рішення власника (ДЖЕРЕЛО ІСТИНИ, не відхиляйся від нього)
- O:\projects\statok\specs\_draft-data-arch.md — вже готова архітектурна частина ТЗ (модель даних, API, сервіси) — УЗГОДЖУЙСЯ з нею в назвах сутностей/полів/ендпоінтів, не дублюй її зміст
- O:\projects\statok\research\deployment-blueprint.md — стек/структура для контексту

Напиши файл O:\projects\statok\specs\_draft-functional.md українською. Функціональні вимоги Фази 1 по модулях; кожна вимога має ID (FR-NN) та acceptance criteria (тестовні чекліст-пункти «- [ ]»).

КАНОН (узгоджений з архітектурним документом — звіряйся з ним):
- Сутності: accounts, assets, bond_details, transactions, price_quotes, fx_rates, net_worth_snapshots. Позиції (holdings) — похідні з транзакцій, окремої таблиці нема.
- Типи транзакцій: buy, sell, deposit, withdraw, transfer_out/transfer_in (звʼязані через transfer_group_id), dividend, coupon, interest, split, ticker_change, opening_balance. Комісія — поле fee на buy/sell (НЕ окремий тип транзакції).
- Типи активів: stock, etf, crypto, bond, cash (готівка/депозит = cash-актив у відповідній валюті).
- Гроші: фіат-суми — integer minor units + код валюти; кількості/ціни — numeric. Базова валюта USD (env BASE_CURRENCY).
- Дивіденди/купони: поля брутто / утриманий податок / нетто.

Модулі для покриття (окремий розділ на кожен):
1. Автентифікація: логін/пароль + JWT, сід адміна з env, rate-limit на логін.
2. Рахунки: CRUD; тип broker/exchange/bank/wallet/cash; мультивалютні кеш-залишки на рахунку (рахунки власника в UAH/USD/EUR).
3. Активи: каталог; створення з тікером/валютою; для bond — bond_details (номінал, валюта, купонна ставка, частота виплат, дата погашення).
4. Транзакції: CRUD усіх типів; валідації (не продати більше ніж є тощо); комісія включається в собівартість купівлі / зменшує виручку продажу; перекази строго парою; opening_balance — механізм «поточного стану»: завести наявну позицію (кількість + опційна собівартість) або кеш-залишок без історії; редагування/видалення → перерахунок похідних.
5. Облігаційний модуль — НАЙВИЩИЙ ПРІОРИТЕТ (власник зараз тримає лише ОВДП): купонний розклад із ставки/частоти, очікувані майбутні виплати, фіксація купона (coupon transaction з податком), автопогашення в кеш на дату maturity, YTM і поточна дохідність.
6. Ціни: денна джоба Yahoo Finance (акції/ETF/крипта через пари типу BTC-USD) + ручне редагування ціни з manual-флагом (джоба не перетирає ручні); історія цін.
7. Курси валют: денні Frankfurter/ECB + НБУ (UAH), історія в БД; конвертація всього в базову валюту; fallback — останній наявний курс.
8. Оцінка портфеля і P&L: середньозважена собівартість; realized/unrealized P&L (абсолют і %); розрізи по рахунку/активу/портфелю; усе конвертується в базову.
9. Net worth снапшоти: денна джоба (EOD ціни+курси) + кнопка ручного перерахунку.
10. Екрани: дашборд net worth у часі (графік зі снапшотів; періоди 1м/3м/1р/все) + звіт грошових потоків (внески/виведення/дивіденди/купони по місяцях). Операційні екрани: список рахунків із балансами, позиції рахунку (з поточною ціною і P&L), журнал транзакцій із фільтрами, форми вводу. Мобільний адаптив.
11. CSV-експорт: транзакції та позиції.
12. Бекап: кнопка «завантажити дамп БД» в UI (нічний бекап — лише згадка, деталі в арх. документі).
13. Налаштування: мова (укр дефолт / англ), тема (світла/темна/системна).
14. PWA: маніфест, інсталяція, офлайн app shell.

Додай розділ «Нефункціональні вимоги»: приватність (вичерпний список дозволених зовнішніх викликів: Yahoo, Frankfurter, НБУ, Telegram-нотифай деплою), безпека, продуктивність (1 користувач, до ~20k транзакцій, відповіді < 500мс), надійність джоб (ретраї, ідемпотентність), i18n, базова доступність.

Додай розділ «Поза скоупом v1» (явний перелік): CSV-імпорт транзакцій, API брокерів/бірж, XIRR/TWR/бенчмарки, алокаційний дашборд, цінові алерти, податкові звіти, витрати/бюджети (фаза 2), multi-user, in-kind перекази активів, періодичні збори брокера, CoinGecko.

Стиль: конкретика, таблиці де доречно, acceptance criteria як чекліст. Без води. НЕ дублюй схему БД/ендпоінти/деплой з арх. документа — посилайся на нього.

Фінальним повідомленням поверни (НЕ вміст файлу): 5–10 рядків — ключові продуктові рішення, які ти ухвалив у деталях, і відкриті питання, якщо лишились.
```

### 2. Зібрати фінальне ТЗ — `specs/statok-tz.md` (сам, без підагента)

Структура за `prompts/02-spec-from-requirements.md`: (1) контекст і цілі; (2) goals/non-goals; (3) фази й MVP-cut; (4) функціональні вимоги — влити `_draft-functional.md`; (5) модель даних + (6) нефункціональні + (7) архітектура — влити `_draft-data-arch.md`; (8) посилання на бэклог; (9) ризики/відкриті питання (включно зі спрощеннями v1 згаданими вище). Звір узгодженість назв сутностей/ендпоінтів між частинами — це дві різні руки писали.

### 3. Бэклог + адверсаріальне ревю — ДВА ПАРАЛЕЛЬНІ підагенти

- **Бэклог-агент** (вхід: `specs/statok-tz.md`): пише `tasks/backlog.md` — епіки → задачі з ID `ST-NNN`; кожна задача: назва, стислий опис, очікувані файли/шляхи, залежності (ID), acceptance criteria, фаза (scaffold/data/backend/frontend/verify/deploy), рекомендована модель (`sonnet` для простих фронт-екранів). Формат придатний для менеджер-агента Workflow (`prompts/03-manager-workflow.md`).
- **Ревю-агент** (вхід: `requirements.md` + `statok-tz.md`): адверсаріально шукає суперечності з вимогами, дірки/нетестовність acceptance criteria, неузгодженість назв між функціональною та арх-частинами, нереалізовні місця. Повертає список знахідок із серйозністю (critical/major/minor); сам нічого не править.

### 4. Фіналізація

Виправ ТЗ за critical/major знахідками ревю (узгодь бэклог, якщо зачеплено) → видали `specs/_draft-*.md` → закоміть (`specs/statok-tz.md` + `tasks/backlog.md`) → Telegram-пінг «ТЗ готово» → стисле резюме в чат: що в ТЗ, скільки задач у бэклозі, знахідки ревю, наступний крок.

## Далі по пайплайну (НЕ робити в цій сесії без явної команди)

Крок 3 — імплементація за `prompts/03-manager-workflow.md` (менеджер через `Workflow` + підагенти по фазах). Workflow вимагає явного opt-in користувача. Деплой — патерн tardis (`research/deployment-blueprint.md`): прод не піднімати, генерувати CI/CD+інфру, ручні кроки за користувачем (секрети `/opt/statok/.env`, DNS `statok.simk.in.ua`).

## Конвенції та застереження

- Коміти локально по ходу, БЕЗ push; трейлер `Co-Authored-By: Claude <noreply@anthropic.com>`.
- Користувач любить паралельних підагентів; прості таски — мінімальний промпт.
- **Якщо підагент впав/перерваний: СПОЧАТКУ перевір, що він реально записав на диск і скільки токенів спожив. Готові артефакти — священні: комітити, використовувати, не перегенеровувати. Перезапуск — лише якщо доробку справді нема, і поясни це користувачу одним реченням ДО запуску.** Якщо доступний механізм продовження агента (SendMessage) — спершу він.
- Windows; PowerShell 5.1 (без `&&`) і bash. Дроп-портів уникати: 3333/3001/8081/5006/5000 зайняті чужими docker-контейнерами.
- Telegram-пінги: `bash "$HOME/.claude/skills/notify-me/scripts/notify.sh" "<текст>"`.
